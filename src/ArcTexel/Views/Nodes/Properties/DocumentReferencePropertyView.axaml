<properties:NodePropertyView xmlns="https://github.com/avaloniaui"
                             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                             xmlns:properties="clr-namespace:ArcTexel.Views.Nodes.Properties"
                             xmlns:ui="clr-namespace:ArcTexel.Extensions.UI;assembly=ArcTexel.Extensions"
                             xmlns:converters="clr-namespace:ArcTexel.Helpers.Converters"
                             xmlns:localization="clr-namespace:ArcTexel.UI.Common.Localization;assembly=ArcTexel.UI.Common"
                             xmlns:input="clr-namespace:ArcTexel.Views.Input"
                             xmlns:settings="clr-namespace:ArcTexel.Views.Tools.ToolSettings.Settings"
                             xmlns:blackboard="clr-namespace:ArcTexel.ViewModels.Document.Blackboard"
                             xmlns:properties1="clr-namespace:ArcTexel.ViewModels.Nodes.Properties"
                             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
                             x:Class="ArcTexel.Views.Nodes.Properties.DocumentReferencePropertyView">
    <Design.DataContext>
        <properties1:DocumentReferencePropertyViewModel />
    </Design.DataContext>
    <StackPanel Orientation="Horizontal"
                HorizontalAlignment="{Binding IsInput, Converter={converters:BoolToValueConverter FalseValue='Right', TrueValue='Stretch'}}">
        <TextBlock localization:Translator.TooltipKey="{Binding DisplayName}" VerticalAlignment="Center"
                   localization:Translator.Key="{Binding DisplayName}" />
        <Button Command="{Binding PickGraphFileCommand}"
                MaxWidth="100"
                Margin="5, 0, 0, 0">
            <Panel>
                <TextBlock FontSize="12" localization:Translator.Key="SELECT" IsVisible="{Binding OriginalFilePath, Converter={converters:EmptyStringConverter}}" />
                <TextBlock TextTrimming="CharacterEllipsis" FontSize="12"
                           Text="{Binding OriginalFilePath, Converter={converters:FilePathToFileNameConverter}}"/>
            </Panel>
        </Button>
    </StackPanel>
</properties:NodePropertyView>